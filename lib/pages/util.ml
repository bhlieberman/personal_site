open Tyxml.Html

let on_click = Unsafe.string_attrib "hx:on-click"

let trigger = Unsafe.string_attrib "hx-trigger"

let target = Unsafe.string_attrib "hx-target"

let swap = Unsafe.string_attrib "hx-swap"